import { FileUp, Fingerprint, Link2, QrCode, UserCheck } from "lucide-react"; import { Card, CardHeader } from "@/components/ui/card";
const steps = [{ icon: FileUp, title: "Upload Report", text: "Add a patient's PDF" }, { icon: Fingerprint, title: "Generate UID", text: "A unique ID is issued" }, { icon: Link2, title: "Secure Link", text: "Private access is created" }, { icon: QrCode, title: "Generate QR", text: "Ready for easy sharing" }, { icon: UserCheck, title: "Patient Access", text: "View and download safely" }];
export function HowItWorks() { return <Card><CardHeader title="How it works" description="From upload to verified patient access in seconds"/><div className="grid gap-6 p-6 sm:grid-cols-2 xl:grid-cols-5">{steps.map(({ icon: Icon, title, text }, i) => <div key={title} className="relative text-center"><div className="mx-auto grid size-16 place-items-center rounded-full border border-gold/20 bg-amber-50/50 text-amber-600"><Icon size={25}/></div><span className="mt-3 block text-xs font-semibold text-gold">0{i+1}</span><h3 className="mt-1 text-sm font-semibold">{title}</h3><p className="mt-1 text-xs leading-5 text-slate-500">{text}</p>{i < 4 && <div className="absolute left-[70%] top-8 hidden h-px w-[60%] bg-gradient-to-r from-gold/60 to-transparent xl:block"/>}</div>)}</div></Card>; }
