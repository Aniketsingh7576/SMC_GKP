import { cn } from "@/lib/utils";
export function Badge({ children, tone = "green", className }: { children: React.ReactNode; tone?: "green" | "gold" | "gray" | "red"; className?: string }) { return <span className={cn("inline-flex items-center rounded-full px-2.5 py-1 text-xs font-semibold capitalize", { "bg-emerald-50 text-emerald-700": tone === "green", "bg-amber-50 text-amber-700": tone === "gold", "bg-slate-100 text-slate-600": tone === "gray", "bg-red-50 text-red-700": tone === "red" }, className)}>{children}</span>; }
