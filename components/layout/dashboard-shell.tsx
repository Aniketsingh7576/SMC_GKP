"use client";
import { useState } from "react"; import { Bell, Menu } from "lucide-react"; import { Sidebar } from "./sidebar"; import { GlobalSearch } from "./global-search"; import type { SessionUser } from "@/types";
export function DashboardShell({ user, children }: { user: SessionUser; children: React.ReactNode }) { const [open, setOpen] = useState(false); return <div className="min-h-screen bg-canvas"><Sidebar user={user} open={open} onClose={() => setOpen(false)} />{open && <div onClick={() => setOpen(false)} className="fixed inset-0 z-40 bg-slate-950/40 backdrop-blur-sm lg:hidden"/>}<div className="lg:pl-[272px]"><header className="sticky top-0 z-30 flex h-[76px] items-center gap-4 border-b bg-white/85 px-5 backdrop-blur-xl sm:px-8"><button onClick={() => setOpen(true)} className="lg:hidden"><Menu /></button><GlobalSearch/><div className="ml-auto flex items-center gap-4"><button className="relative grid size-10 place-items-center rounded-full hover:bg-slate-100"><Bell size={19}/><span className="absolute right-2 top-2 size-2 rounded-full bg-gold"/></button><div className="hidden text-right sm:block"><p className="text-xs text-slate-500">Welcome back,</p><p className="text-sm font-semibold">{user.name}</p></div><div className="grid size-10 place-items-center rounded-full bg-ink text-white">{user.name[0]}</div></div></header><main className="p-5 sm:p-8 lg:p-10">{children}</main></div></div>; }
