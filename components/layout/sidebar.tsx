"use client";
import Link from "next/link"; import { usePathname, useRouter } from "next/navigation"; import { Activity, BarChart3, FilePlus2, Files, LayoutDashboard, LogOut, Settings, Users, X } from "lucide-react"; import { cn } from "@/lib/utils"; import { Brand } from "./brand"; import type { SessionUser } from "@/types";
const links = [{ href: "/dashboard", label: "Dashboard", icon: LayoutDashboard }, { href: "/reports/upload", label: "Upload Report", icon: FilePlus2 }, { href: "/reports", label: "All Reports", icon: Files }, { href: "/patients", label: "Patients", icon: Users }, { href: "/analytics", label: "Analytics", icon: BarChart3 }, { href: "/activity", label: "Activity Logs", icon: Activity }, { href: "/settings", label: "Settings", icon: Settings }];
export function Sidebar({ user, open, onClose }: { user: SessionUser; open: boolean; onClose(): void }) { const pathname = usePathname(); const router = useRouter(); async function logout() { await fetch("/api/auth/logout", { method: "POST" }); router.push("/login"); router.refresh(); } return <aside className={cn("fixed inset-y-0 left-0 z-50 flex w-[272px] flex-col bg-navy-glow px-4 py-7 text-white transition-transform lg:translate-x-0", open ? "translate-x-0" : "-translate-x-full")}><button onClick={onClose} className="absolute right-4 top-4 lg:hidden"><X /></button><div className="px-3 pb-9"><Brand /></div><nav className="space-y-1.5">{links.map(({ href, label, icon: Icon }) => { const active = pathname === href || (href !== "/dashboard" && pathname.startsWith(href)); return <Link key={href} href={href} onClick={onClose} className={cn("relative flex items-center gap-3 rounded-xl px-4 py-3.5 text-sm transition", active ? "border border-white/10 bg-white/10 text-white shadow-lg" : "text-slate-300 hover:bg-white/5 hover:text-white")}>{active && <span className="absolute -left-4 h-8 w-1 rounded-r bg-gold" />}<Icon size={19} className={active ? "text-gold" : ""} />{label}</Link>; })}</nav><div className="mt-auto rounded-2xl border border-white/10 bg-white/[.06] p-3"><div className="flex items-center gap-3"><div className="grid size-10 shrink-0 place-items-center rounded-full border border-gold/40 bg-slate-800 font-display">{user.name[0]}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{user.name}</p><p className="capitalize text-xs text-slate-400">{user.role.replace("_", " ")}</p></div><button onClick={logout} title="Sign out" className="text-slate-400 hover:text-white"><LogOut size={17} /></button></div></div></aside>; }
